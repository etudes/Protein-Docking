#
OBJ=	lib.o ../gklib/osx_kleylib
INCL=	Makefile_osx lib.incl

fc =	 g77

exec =	lib

opt= -C -O -u -check_bounds
link= -O

$(exec):	$(OBJ)
	$(fc) $(link) -o $(exec) $(OBJ) ; strip $(exec)

lib.o:	lib.f $(INCL)
	$(fc) $(opt) -c lib.f

